1. ƒан код:

var a = 1, b = 1, c, d;
c = ++a; alert(c);           // 2
d = b++; alert(d);           // 1
c = (2+ ++a); alert(c);      // 5
d = (2+ b++); alert(d);      // 4
alert(a);                    // 3
alert(b);                    // 3

ѕочему код даЄт именно такие результаты?

1. ѕеременной с присваетс€ значение инкрементированной переменной а (1 + 1 = 2)
2.  огда плюсики указана справа от переменной, то сперва переменной d присваиваетс€ значение переменной b, а затем происходит инкрементаци€ b
3. ѕлюсики указаны слева от переменной a, поэтому сперва переменна€ а инкрементируетс€ (2 + 1), а затем суммируетс€ (2 + 3)
4. ѕлюсики справа, поэтому сперва суммируютс€ значени€ b и 2 (d = 2 + 2), а затем инкрементируетс€ b (2 + 1 = 3)
5. »значально a = 1, инкрементировалось дважды (в первом и третьем примерах). ¬ итоге a = 3
6. »значально b = 1, инкрементировалось дважды (во втором и четвертом примерах). ¬ итоге b = 3



